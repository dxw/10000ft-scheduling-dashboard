%div.project{ class: project_classes(project: project) }
  %h2= project.name.downcase.titleize
  %ul.team-members
    - assignments.each do |assignment|
      - user = users.detect{ |user| user.id == assignment.user_id }
      %li.member
        = render 'user', user: user
